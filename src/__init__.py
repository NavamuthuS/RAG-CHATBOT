"""
src/__init__.py — Package Initializer

This file makes the `src` directory a proper Python package,
allowing imports like:
    from src.document_loader import load_documents
    from src.rag_chain import build_rag_chain
"""

# No code needed here — the file's presence is enough to mark
# this directory as a Python package.